@model IEnumerable<VenueManagment.Models.TipoEvento>

@{
    ViewData["Title"] = "Tipo Evento";
}

<h1></h1>

<p>
    <a asp-action="Create" class='btn btn-info text-white'>Crear Nuevo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.estado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.maestroevento)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.estado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.maestroevento.nombre)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idTipoEvento">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.idTipoEvento">Detalles</a> |
                <a asp-action="Delete" asp-route-id="@item.idTipoEvento">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
